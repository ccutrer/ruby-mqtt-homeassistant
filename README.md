## Validation

Limited validations are applied by default.
To perform a full validation with the same Python configuration engine that Home Assistant uses, you must include the `pycall` gem in your project, and ensure you have a venv activated that has the Python version from `.python-version` and the dependencies from `requirements.txt` installed.
